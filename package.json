{
	"scripts": {
		"format": "npm run format:js && npm run format:html",
		"format:js": "biome format --write frontend/**/*.js",
		"format:html": "prettier --write frontend/**/*.html",
		"lint": "biome lint .",
		"check": "biome check --write ."
		
		
	},
	"devDependencies": {
		"@biomejs/biome": "2.3.4",
		"prettier": "^3.6.2"
	}
}
